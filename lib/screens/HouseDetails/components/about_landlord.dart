import 'package:flutter/material.dart';

class AboutLandlord extends StatelessWidget {
  const AboutLandlord({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}