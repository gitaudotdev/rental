import 'package:flutter/material.dart';

class AboutSpace extends StatelessWidget {
  const AboutSpace({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}